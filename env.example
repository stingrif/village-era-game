# Скопируйте в .env и подставьте URL бэкенда:
# cp env.example .env
# Затем: node generate_config.js  (чтобы подставить API_BASE в config.js для игры)

# Пусто = игра без API (только localStorage).
# Локально: http://localhost:8000
# Продакшен: https://ваш-домен.com
API_BASE=http://localhost:8000
