# Скопировать в .env.secrets и заполнить: cp env.secrets.example .env.secrets
# Контейнер secrets читает этот файл. Auth и sessions тоже получают env из него.
# Для независимости при падении Secrets: TELEGRAM_BOT_TOKEN и SESSION_SIGNING_KEY
# могут быть заданы в env auth/sessions — тогда они не обращаются к Secrets за этими ключами.
SECRETS_INTERNAL_TOKEN=your_internal_token
SECRET_KEYS=INTERNAL_API_SECRET,TELEGRAM_BOT_TOKEN,SESSION_SIGNING_KEY
INTERNAL_API_SECRET=
TELEGRAM_BOT_TOKEN=
SESSION_SIGNING_KEY=
