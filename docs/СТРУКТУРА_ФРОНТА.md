# Структура фронта и зависимости (по аналогии с НОВЫЙ)

Кратко: папки, порядок загрузки, доступы, связь с механиками (Инструкция).

## Папки

| Путь | Назначение |
|------|------------|
| `frontend/config/` | Конфиг фич: `features.js` → `GAME_FEATURES` (screens, marketOrders, aboutCatalog) |
| `frontend/data/` | Данные для экранов: `help-content.js` → `HELP_SECTIONS` (обучение) |
| `frontend/js/` | Модули экранов: `about.js`, `market.js`, `help.js` |
| `data/` (корень) | Статический каталог: `items-catalog.json` (если нет API) |

## Зависимости (порядок скриптов в HTML)

1. `config.js` — API_BASE, базовые настройки.
2. `frontend/config/features.js` — флаги экранов и фич.
3. `frontend/js/market.js` — маркет (ордера), при `marketOrders: true`.
4. `frontend/js/about.js` — «О проекте» с вкладками и каталогом, при `aboutCatalog: true`.
5. `frontend/data/help-content.js` — контент обучения (все секции гайда).
6. `frontend/js/help.js` — подмена `G.openHelp`, рендер обучения из `HELP_SECTIONS`.

## Доступы

- **Кнопка 📖 (Обучение):** вызывает `G.openHelp()`. Реализация из `help.js`: сборка HTML из `HELP_SECTIONS`, открытие `#helpModal`.
- **Кнопка ℹ️ (О проекте):** `G.openAboutProject()` — из основного скрипта или `about.js` (вкладки + каталог).
- Экраны (деревня, шахта, маркет, профиль, задания и т.д.) управляются из основного скрипта; флаги `GAME_FEATURES.screens` можно использовать для скрытия пунктов меню.

## Механики (источники)

Описание механик игры — в папке **Инструкция/**:

| Тема | Файл |
|------|------|
| Здания, слоты, задания | 01_Здания_и_задания.md |
| Реликвии, эффекты, allowedBuildings | 02_Реликвии_и_эффекты.md |
| Шахта, яйца | 03_Шахта_и_яйца.md |
| Бафы, порчи, Defense Tier | 04_Бафы_и_порчи.md |
| Крафт, слияние | 05_Крафт_и_слияние.md |
| Падение неба | 06_Падение_неба.md |
| Вывод, правило 10% | 08_Вывод_и_антихалява.md |
| Рынок, P2P | 11_Рынок_и_P2P.md |
| Квест ФЕНИКС | 17_Скрытый_квест_ФЕНИКС.md |
| Зависимости механик | 20_Зависимости_механик.md |

Контент экрана «Обучение» (`frontend/data/help-content.js`) собран по этим инструкциям и каталогу предметов; при изменении механик достаточно обновить `HELP_SECTIONS` и при необходимости инструкции.

## Отключение модулей

- **Обучение (полный гайд):** не подключать `help-content.js` и `help.js` или `screens.help: false` → кнопка 📖 покажет fallback.
- **Маркет (ордера):** `marketOrders: false`.
- **Каталог во «О проекте»:** `aboutCatalog: false`.

Подробнее: `frontend/README.md`.
