# Модульная структура фронта (Village Era)

Структура папок и зависимостей — по аналогии с проектом НОВЫЙ: конфиг фич, экраны, данные, механики.

## Структура папок

```
frontend/
├── config/
│   └── features.js      # Включение/отключение экранов и фич (screens, marketOrders, aboutCatalog)
├── data/
│   └── help-content.js   # Контент экрана «Обучение» (HELP_SECTIONS) — гайд для новичков, предметы, бафы, механики
├── js/
│   ├── about.js          # Модалка «О проекте»: вкладки + каталог предметов (~121)
│   ├── market.js         # Маркет: ордера API, рендер, покупка по ордеру
│   └── help.js           # Экран «Обучение» (📖): сборка HTML из HELP_SECTIONS, G.openHelp
└── README.md             # Этот файл
```

## Зависимости (порядок загрузки в HTML)

1. **config.js** (корень) — базовый API_BASE и т.п.
2. **frontend/config/features.js** — `window.GAME_FEATURES` (screens, marketOrders, aboutCatalog).
3. **frontend/js/market.js** — при `marketOrders: true` вешает логику маркета на `G`.
4. **frontend/js/about.js** — при `aboutCatalog: true` подменяет `G.openAboutProject` / `G.switchAboutTab`.
5. **frontend/data/help-content.js** — `window.HELP_SECTIONS` (массив секций обучения).
6. **frontend/js/help.js** — подменяет `G.openHelp` на функцию, которая рендерит обучение из `HELP_SECTIONS` и открывает модалку.

Итог: кнопка 📖 в шапке вызывает `G.openHelp()`; реализация из `help.js` заполняет `#helpmc` и открывает `#helpModal`.

## Доступы и экраны

| Экран   | Флаг в GAME_FEATURES   | Файлы / механика |
|---------|------------------------|-------------------|
| Деревня | `screens.village`      | Основной скрипт (G), здания, слоты |
| Шахта   | `screens.mine`         | Основной скрипт, попытки, чекин |
| Инвентарь | `screens.inventory`   | Основной скрипт |
| Маркет  | `screens.market` + `marketOrders` | `market.js`, API ордеров |
| Профиль | `screens.profile`     | Основной скрипт, авторизация |
| О проекте | `aboutCatalog`       | `about.js`, вкладки + каталог |
| **Обучение** | **`screens.help`** | **`data/help-content.js` + `js/help.js`** |

Отключение: в `config/features.js` выставить `screens.help: false` или не подключать `help-content.js` / `help.js` — тогда при нажатии 📖 останется fallback из основного скрипта («Загрузка…»).

## Контент обучения (help-content.js)

Источники: Инструкция (01–21), каталог предметов, зависимости механик.

- Для новичков, эра и деревня (9 слотов, 32 здания, масштаб реликвий).
- Шахта (попытки, чекин, алмазы).
- Реликвии: Fire / Yin / Yan / Tsy / Magic / Epic, таблицы эффектов и allowedBuildings.
- Как комбинировать (доход, защита, PvP, слияние).
- Бафы и порчи (качество, длительность, классы weak/strong/severe).
- Defense Tier (веса зданий, Tier 2/3, артефакты anti-curse).
- Крафт и слияние (Merge 2/3, Upgrade, Reroll, Dust / Coins+Stars).
- Зависимости механик (здания → механики, цепочка прогрессии).
- Падение неба (Skyfall), маркет, профиль, вывод, сжигание, квест ФЕНИКС, поинты и задания.

Полный каталог предметов (~121) — в «О проекте» (ℹ️), вкладки Реликвии / Амулеты / Бафы / Порчи / Артефакты / Яйца.

## Как отключить

- **Обучение (полный гайд):** не подключать `help-content.js` и `help.js` или в `features.js` задать `screens.help: false`. Кнопка 📖 покажет fallback «Загрузка…».
- **Маркет (ордера):** `marketOrders: false`.
- **Каталог во «О проекте»:** `aboutCatalog: false`.

Каталог предметов: приоритетно `GET /api/game/items-catalog`, иначе статический `data/items-catalog.json`.
